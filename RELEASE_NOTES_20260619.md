# SOR2XCombo 更新说明

**版本:** SOR2XCombo_20260619 (V.2.3.1 B.20260619)

---

## 本次更新概要

修复 Boss / 敌人在血量已归零但尚未进入死亡状态时, 仍会继续执行 AI、闪避、隐身、投技等脚本逻辑, 导致无法选中、原地旋转、关卡卡死等 **僵尸实体软锁** 问题.

---

## 主要改动

### 1. 新增统一存活判定

在脚本层加入 `entityAlive()` / `selfAlive()` 辅助函数, 判定条件为:

```text
实体存在 && dead == 0 && health > 0
```

不再只依赖 `dead == 0`, 避免「血已空但还没死」的中间状态继续跑逻辑.

### 2. 全脚本层防护

以下脚本层在关键入口增加了存活判定:

- **动画脚本** — 取消、移动、闪避、投技相关 `@cmd`
- **Think / AI** — 闪避、追击、召唤、投技判定等
- **Updateentity** — 残影、闪烁、OTG、能量恢复、抓取检测等
- **Didhit** — 自定义投技、落地伤害、连段恢复等

### 3. 投技中途死亡紧急释放

- 抓取方或被抓方在投技动画中死亡时, 会自动解除绑定并解除冻结
- 避免一方已死、另一方仍被 `grabbed` 变量锁死的卡关情况

### 4. Neo X 相关

- 移除了 Neo X 专用的 `deathGuard()` 兜底脚本
- 改由上述统一存活判定覆盖, 行为更一致

### 5. 脚本编译修复

- 修复部分角色 / 武器加载时报 `can't find function 'selfAlive'` 等编译错误
- 涉及 Flash 投技脚本、ftkunai 武器、Axel 摩托车等

---

## 推荐测试重点 (社区测试用)

如果你愿意帮忙进一步测试, 请重点留意以下场景:

1. **Neo X 对战** — 用 Zan 等特殊技击杀, 确认 Boss 正常死亡, 不会继续闪避或隐身
2. **投技中途击杀** — 抓取方或被抓方在投技动画中被打死, 确认双方都能正常脱离, 不会卡死
3. **Tracker** — 父实体死亡后, 隐身 / 瞄准 / 离子炮是否还会异常触发
4. **跳跃投技 / 自定义抓取** — 多角色、多 Boss 下是否正常
5. **Adam 保龄球连段** — 对即将死亡的敌人是否还会异常弹回
6. **SOR3 摩托车关 (Axel_Bike)** — 确认能正常加载并游玩

---

## 已知说明

- 本版本为 **Bugfix 测试版**, 作者已做几轮简单自测, 未发现新问题
- 若遇到脚本编译失败、软锁、无法选中等问题, 请保存 `Logs/OpenBorLog.txt` 并反馈

---

## 反馈渠道

- QQ 群: **657798865**
- B 站: [iintothewind](https://space.bilibili.com/29372518)

反馈时请尽量附上:

- 使用的版本号 (SOR2XCombo_20260619)
- 复现步骤
- `Logs/OpenBorLog.txt` 日志文件

感谢测试!
